name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.26.1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Luis Dharna
        env:
          BASE_PATH: /luis-dharna/
          PORT: 4173
          NODE_ENV: production
        run: pnpm --filter @workspace/luis-dharna run build

      - name: Prepare Pages
        run: |
          rm -rf pages
          mkdir -p pages

          DIST="artifacts/luis-dharna/dist/public"

          echo "Usando build:"
          echo "$DIST"

          if [ ! -f "$DIST/index.html" ]; then
            echo "ERROR: No se encontró $DIST/index.html"
            echo ""
            echo "Buscando index.html:"
            find . -type f -name "index.html" -not -path "./node_modules/*"
            exit 1
          fi

          cp -R "$DIST"/. pages/

          echo ""
          echo "=== CONTENIDO DE PAGES ==="
          find pages -maxdepth 3 -type f

          echo ""
          echo "=== INDEX.HTML GENERADO ==="
          cat pages/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./pages

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    needs: build

    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
